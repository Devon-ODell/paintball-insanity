# Rokit toolchain manifest.
# NOTE: wally 0.3.2 ships an x86_64-only macOS binary and will not run on
# Apple Silicon without Rosetta 2. See docs/PLATFORM_NOTES.md.
[tools]
rojo = "rojo-rbx/rojo@7.7.0"
wally = "UpliftGames/wally@0.3.2"
lune = "lune-org/lune@0.10.5"
